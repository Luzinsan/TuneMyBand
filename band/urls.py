from django.urls import path, include
from . import views

app_name = 'band'
urlpatterns = [
    # path('<int:pk>/', views.DetailView.as_view(), name='detail'),
    # path("equipments/", views.BandEquipmentList.as_view(), name='music_band_equipments'),
    # path('', views.index, name='index'),
]
